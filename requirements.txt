# Same versions as the RMM server, so the two products stay on one stack.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
itsdangerous==2.2.0
cryptography==44.0.0
msal==1.31.1
python-multipart==0.0.20
